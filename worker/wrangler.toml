name = "anchor-sync"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Created via `wrangler kv namespace create SYNC_KV` — paste the printed id below.
kv_namespaces = [
  { binding = "SYNC_KV", id = "REPLACE_WITH_KV_NAMESPACE_ID" }
]
